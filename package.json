{
  "name": "state-sdk-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "build:deploy": "turbo run build && mkdir -p .vercel-output && cp -r examples/showcase/dist/* .vercel-output/ && mkdir -p .vercel-output/counter && cp -r examples/counter/dist/* .vercel-output/counter/"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
